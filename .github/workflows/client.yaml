on: [push]

jobs:
  mi-job:
    uses: alonsoM10/AUY1104-SharedWorkflows/.github/workflows/deploy-api.yaml@pipeTest
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}